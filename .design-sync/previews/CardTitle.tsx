import { Card, CardHeader, CardTitle } from "reece-dashboard";

export const InContext = () => (
  <div style={{ maxWidth: 360 }}>
    <Card>
      <CardHeader>
        <CardTitle>Workflow Health</CardTitle>
      </CardHeader>
    </Card>
  </div>
);
